<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".fragments.userslist.presentation.UsersListFragment">

    <data>

        <variable
            name="vm"
            type="com.zywczas.sliide.fragments.userslist.presentation.UsersListViewModel" />

        <variable
            name="usersAdapterXML"
            type="com.mikepenz.fastadapter.FastAdapter" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary">
            <!--todo add theme, text color, etc..-->
            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:title="@string/users_title" />
        </com.google.android.material.appbar.AppBarLayout>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/constraintLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">
            <!--todo add theme - color, rounded corners...-->
            <com.google.android.material.progressindicator.LinearProgressIndicator
                android:id="@+id/progressBar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/linearProgressBarHorizontalMargin"
                android:layout_marginTop="@dimen/padding_minimum"
                android:layout_marginEnd="@dimen/linearProgressBarHorizontalMargin"
                android:visibility="gone"
                app:isVisible="@{vm.isProgressBarVisible()}"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:visibility="visible" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/usersList"
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:adapter="@{usersAdapterXML}"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/progressBar"
                tools:itemCount="3"
                tools:listitem="@layout/item_user" />
        </androidx.constraintlayout.widget.ConstraintLayout>
<!--todo change theme-->
        <com.leinardi.android.speeddial.SpeedDialView
            android:id="@+id/speedDial"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:scaleType="centerCrop"
            android:theme="@style/Theme.Sliide"
            app:fabSize="normal"
            app:layout_behavior="@string/speeddial_scrolling_view_snackbar_behavior"
            app:sdMainFabAnimationRotateAngle="0"
            app:sdMainFabClosedSrc="@drawable/ic_plus"
            app:sdMainFabOpenedSrc="@drawable/ic_plus"
            app:sdUseReverseAnimationOnClose="true" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
